import { Component } from "../ECS";

export class Script extends Component {
  onLoad() {
  }

  onUpdate(deltaTime, elapsedTime) {
  }

  onKeyDown(e) {
  }

  onKeyUp(e) {
  }

  onMouseMove(e) {
  }
}